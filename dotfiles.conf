DOTFILES=(
    .aliases
    .antigen
    .bashrc
    .config/nvim
    .exports
    .gitconfig
    .gvimrc
    .local/share/nvim
    .tmux.conf
    .vim
    .vimrc
    .xprofile
    .zshrc
)
DOTFILES_ROOT="$(dirname "${BASH_SOURCE[0]}")"
